# Commit Planning — one assignment item = one commit

The PR's headline artifact is a readable list of changes: a reviewer opening the *Commits* tab must see **one commit per point the assignment asked for**, and nothing else. Build that plan before writing any code, applying the **one phase = one commit** rule from `@rules/git/general.mdc` *Git Rules* — a phase is simply one kind of assignment item.

1. **Extract the item list from the assignment.** Walk the issue description, the *current requirements* kept by comment analysis, and every attachment / linked source, and collect each discrete point to solve. Any of these is an item marker:
   - an explicit recommendation block — *Doporučené opravy*, *Doporučení*, *Body k řešení*, *Recommended fixes*, *Suggested fixes*, *Action items*, *Návrh řešení*;
   - a checklist (`- [ ]`), a numbered or bulleted list of requirements, or an acceptance-criteria block;
   - review / audit findings carried inside the assignment — one item per finding (Critical / Moderate / Minor rows included);
   - `Phase N` headings, numbered milestones, or a step-by-step plan written by the reporter;
   - a requirement stated only in running prose — read the whole assignment, not just its bullets.

   Record every item as a row: an ID (`A1`, `A2`, … in the order it appears in the assignment), the verbatim quote or heading anchor it came from, and the concrete files / symbols it will touch (taken from the *Read, Map & Verify* pre-flight in `@skills/resolve-issue/SKILL.md`).
2. **Normalize the list.** Merge two rows only when they are literally the same change stated twice (keep both IDs on the merged row); split a row only when one sentence names two independent changes. Never fold two distinct points into one row to shorten the plan, and never invent an item the assignment does not ask for — a finding with no assignment anchor belongs to `@skills/resolve-issue/SKILL.md` *Pre-existing issue handling* or to the deferred `## TODO` list, never to this table.
3. **One item = one commit.** Every in-scope item from the scope split in `@skills/resolve-issue/SKILL.md` *Problem analysis* maps to exactly **one** commit, and no commit carries two items. Phase order (when the assignment is phased) stays the commit order; otherwise assignment order is the default.
4. **Make each commit self-contained.** A commit ships its item's production change **and** everything that item needs to be complete: its regression test (bug) or coverage (feature), plus any migration, locale entry, config key, or factory it introduces. Never defer an item's tests to a later commit.

   **And nothing that nothing yet reaches — every commit is live.** Self-contained is only half the contract: a commit must also ship no code that stays unreachable until a later commit lands, per `@rules/git/general.mdc` *Git Rules* (*Every commit is live*). Plan each item as a **vertical slice** — the new class / method / branch together with the call site that puts it to work and the test that exercises it — never as a horizontal `add the action` commit followed by a `use the action` commit. Only an inert prerequisite (a config key, an off-by-default feature flag, the additive expand step of a schema change) may be planned ahead of its consumer, and the plan records which later commit consumes it; when a symbol cannot be separated from its call site without leaving one of them dead, plan them into **one** commit.

   **Exception — a refactor item.** When the item is a behavior-preserving refactor, `@rules/refactoring/general.mdc` *Test Coverage Contract* wins over the self-contained rule: the missing coverage lands in a dedicated `test(<scope>): cover <area> before refactor` commit **ordered before** the refactor commit (it is that item's groundwork commit, not a second item), and the refactor commit itself modifies no pre-existing test. This mirrors the `refactor(<scope>): pre-existing — …` workflow in `@skills/resolve-issue/SKILL.md` *Pre-existing issue handling*.
5. **Every commit is green — this one is a blocker.** A planned commit must leave the project's gate (`composer build` in this repository) passing **at its own point of the history**, per `@rules/git/general.mdc` *Git Rules* (*Every commit is green*). Two consequences bind the plan: an item's test and the production change that makes it pass are planned into the **same** commit — never a `test(...)` commit that is red until the next one lands — and no commit may carry a `->skip()` / `->todo()` / `markTestIncomplete()` / inverted assertion / buggy-output fixture standing in for behavior a later commit fixes. When the split cannot avoid a red intermediate commit, **do not split**: keep the hunks in one commit and record why.
6. **Aim for cherry-pick independence (target, not a blocker).** Independence is the ordering goal on top of the green requirement above: a commit is independent when it also applies cleanly onto the default branch on its own. Derive each verdict **by reading the code** — apply the independence checks in `@skills/pr-staged-merge-plan/SKILL.md` step 5 (green on its own, no forward reference, no dead code, expand before contract, tests travel with their behavior) rather than restating them, and never materialize a branch to test it. Then:
   - lift shared groundwork that two or more items need into its **own commit ordered first only when that commit is live on its own** — a migration, a config key, or an off-by-default flag qualifies; a helper or a signature change nobody calls yet does not, and it ships **with the first item that uses it**, with the later items recorded as `depends on <that item>`. Never plan a groundwork commit whose only content is a symbol waiting for a caller: that is the dead-code commit step 4 forbids;
   - label every planned commit `independent` or `depends on <ID>` with a one-line reason;
   - when independence would force an artificial split or a broken intermediate state, **keep the dependency** and record it — an honest dependent chain beats a contrived split.
7. **Order the commits:** pre-existing fixes first (per `@skills/resolve-issue/SKILL.md` *Pre-existing issue handling*), then shared groundwork, then independent item commits in assignment order, then dependent chains after what they need.
8. **Record the plan before implementing** as a table — `item ID | commit subject | files | independent / depends on` — with every subject in `type(scope): description` form per `@rules/git/general.mdc` *Commit Messages* (English, lowercase type/scope, no trailing period). This table is the contract for the implementation step in `@skills/resolve-issue/SKILL.md` *Continue* and the source of the PR's change list.
9. **Commit at the end of each item, not at the end of the work.** Run the pre-push fixers and the **whole** gate for that item's changes — the item's own tests plus whatever the project's gate covers — and commit only once it is green. A red working tree is never committed "to be fixed in the next commit": that is the exact history the green-commit invariant forbids. Then start the next item. Never park two items in one commit "to be split later".
10. **Reconcile the history before opening the PR.** This is the assignment-item form of the partition rule in `@rules/git/general.mdc` *Git Rules*. Read `git log --oneline <default-branch>..HEAD` and confirm: every in-scope item has exactly one commit, every commit maps to exactly one item (or is a named pre-existing / groundwork commit), and no commit bundles two items. When the history drifted from the plan, reshape it **before** the PR exists — an agent runtime without interactive git uses a non-interactive path (`git reset --soft <default-branch>` then re-commit one item at a time, or `git rebase --onto`); `git rebase -i` stays the human option, mechanics in `@skills/git-workflow/SKILL.md` — never explain a bundled commit away in the description.
11. **Verify the reconciled history commit by commit before the PR exists.** Reshaping moves every commit, so a green tip proves nothing about the commits below it. Replay the whole range against the gate — `git rebase --exec '<gate command>' <default-branch>` — and repair a failing commit **in place** (amend, then `git rebase --continue`), never with a repair commit appended at the tip. The PR is opened only after the replay finishes without stopping, so every commit in it cherry-picks onto the default branch and deploys without errors.
12. **An item that needs no change is never dropped silently.** When implementation proves an item already satisfied or invalid, produce no commit for it and carry it into the PR change list as `no change needed — <reason>`.
13. **A genuinely atomic assignment stays one commit.** When the assignment names a single point, do not invent items or artificial phases to fill a table.
